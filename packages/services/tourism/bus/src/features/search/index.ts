import { App } from '@780/utils';

import { view } from './search.view';

const search: App<unknown, unknown, unknown> = {
  repository: () => {},
  controller: () => {},
  presenter: () => {},
  view,
};

export default search;
